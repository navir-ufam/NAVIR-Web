import { describe, it } from "vitest";
// import { renderHook } from "@testing-library/react";
// import { useAuth, AuthProvider } from "../context/index";

describe("AuthContext", () => {
  it.skip("fornece estado inicial: user null e isAuthenticated false", () => {});
});
